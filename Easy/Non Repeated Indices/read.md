We have an array of characters like this: ["a", "fc", "ab", "a", "ab", "b"].

Write a function that takes this array as input and returns the indices of the non-repeated characters.

For example, in this example, the expected output should be [1, 5].
